<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window background. Matches `bgCanvas` in app/theme/tokens.ts so the
         activity blends with the theme where it is drawn behind the system
         bars in edge-to-edge mode. -->
    <color name="window_background">#060B12</color>
</resources>
