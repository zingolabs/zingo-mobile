import type { Meta, StoryObj } from '@storybook/react-native';
import SheetRim from './SheetRim';

const meta: Meta<typeof SheetRim> = {
  title: 'Components/SheetRim',
  component: SheetRim,
};

export default meta;
type Story = StoryObj<typeof SheetRim>;

export const Default: Story = {};
