export enum CommandEnum {
  // new commands to create a proposal
  send = 'send',
  sendall = 'send_all',
  shield = 'shield',
  // this works for all: send, sendall & shield
  confirm = 'confirm',
}
