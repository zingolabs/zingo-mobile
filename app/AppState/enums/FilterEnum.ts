export enum FilterEnum {
  all = 'all',
  contacts = 'contacts',
  noContacts = 'no contacts',
  withFunds = 'withFunds',
  wallet = 'wallet',
  // new ones - kind
  sent = 'sent',
  received = 'received',
  shielded = 'shielded',
  swap = 'swap',
  // new one - Status
  failed = 'failed',
  // new one
  memos = 'memos',
}
