name: Jest test

on:
  workflow_call:

jobs:
  jest-test:
    name: Jest test
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - run: yarn

      - run: yarn test

