appId: org.ZingoLabs.Zingo
---
- launchApp:
    clearState: true

# In basic mode the wallet is created automatically.
# The app lands on the Receive screen.
- extendedWaitUntil:
    visible:
      text: "My Receiving Address"
    timeout: 60000

- assertVisible:
    id: "header.drawmenu"
